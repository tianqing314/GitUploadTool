<Window x:Class="GitUploadTool.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="GitUploadTool"
        Height="750" Width="1150"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="#0D1117">

   <Window.Resources>
       <ResourceDictionary>
           <Style x:Key="NavButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="#C9D1D9" />
                <Setter Property="Height" Value="40" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="Padding" Value="12,0" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#21262D" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left Navigation Panel -->
        <Border Grid.Column="0" Background="#161B22">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- User Info -->
                <Border Grid.Row="0" Background="#21262D" Padding="16" Margin="0,0,0,1">
                    <StackPanel>
                        <Ellipse Width="44" Height="44" Margin="0,0,0,8">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="{Binding CurrentUser.AvatarUrl}" />
                            </Ellipse.Fill>
                        </Ellipse>
                        <TextBlock Text="{Binding CurrentUser.Login}"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Foreground="White" />
                        <TextBlock Text="{Binding CurrentUser.Name}"
                                   FontSize="11"
                                   Foreground="#8B949E" />
                        <TextBlock>
                            <Hyperlink NavigateUri="{Binding CurrentUser.HtmlUrl}"
                                       Command="{Binding DataContext.OpenUrlCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                       CommandParameter="{Binding CurrentUser.HtmlUrl}">
                                <TextBlock Text="在GitHub上查看" FontSize="10" Foreground="#0366D6" />
                            </Hyperlink>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Navigation Menu -->
                <StackPanel Grid.Row="1" Margin="0,16,0,0">
                    <Button Content="🏠  首页"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="HomeView">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DataContext.SelectedView, RelativeSource={RelativeSource AncestorType=Window}}" Value="HomeView">
                                        <Setter Property="Background" Value="#30363D" />
                                        <Setter Property="Foreground" Value="White" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="📤  上传"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="UploadView">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DataContext.SelectedView, RelativeSource={RelativeSource AncestorType=Window}}" Value="UploadView">
                                        <Setter Property="Background" Value="#30363D" />
                                        <Setter Property="Foreground" Value="White" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="⚙️  设置"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="SettingsView">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DataContext.SelectedView, RelativeSource={RelativeSource AncestorType=Window}}" Value="SettingsView">
                                        <Setter Property="Background" Value="#30363D" />
                                        <Setter Property="Foreground" Value="White" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="ℹ️  关于"
                            Command="{Binding NavigateCommand}"
                            CommandParameter="AboutView">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource NavButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DataContext.SelectedView, RelativeSource={RelativeSource AncestorType=Window}}" Value="AboutView">
                                        <Setter Property="Background" Value="#30363D" />
                                        <Setter Property="Foreground" Value="White" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

                <!-- Logout Button -->
                <StackPanel Grid.Row="2" Margin="0,0,0,16">
                    <Separator Background="#21262D" Margin="16,0" />
                    <Button Content="🚪  退出登录"
                            Style="{StaticResource NavButton}"
                            Command="{Binding LogoutCommand}"
                            Foreground="#F85149" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Border Grid.Column="1" Background="#0D1117">
            <ContentControl Content="{Binding CurrentView}" />
        </Border>
    </Grid>
</Window>